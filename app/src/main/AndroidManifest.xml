<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ict372ecommerceapp">

        <!-- 1. The LoginActivity is now set as the Starting Screen (Launcher) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. Registration Activity declared once -->
        <activity
            android:name=".RegistrationActivity"
            android:exported="false" />

        <!-- 3. Product Detail Activity declared once -->
        <activity
            android:name=".ProductDetailActivity"
            android:exported="false" />

        <!-- 4. Home Activity -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

        <!-- 5. Cart Activity -->
        <activity
            android:name=".CartActivity"
            android:exported="false" />

        <!-- 6. Checkout Activity -->
        <activity
            android:name=".CheckoutActivity"
            android:exported="false" />

        <!-- 7. Purchase History Activity -->
        <activity
            android:name=".PurchaseHistoryActivity"
            android:exported="false" />

    </application>

</manifest>